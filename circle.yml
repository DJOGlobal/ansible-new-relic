machine:
  services:
    - docker

dependencies:
  override:
    - bundle install:
        timeout: 240
        pwd:
          tests
    - make build:
        pwd:
          tests

test:
  override:
    - make test:
        pwd:
          tests
